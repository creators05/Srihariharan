def multiply_by_unit_digit(numbers):
    reset=[num*(abs(num)%10)for num in numbers]
    return result
numbers=list(map(int,input("enter number seperated by space:").split()))
output=multiply_by_unit_digit(numbers)
print("Result",output)
